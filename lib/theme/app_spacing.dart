import 'package:flutter/material.dart';

class AppSpacing {
  static const pagePadding = EdgeInsets.symmetric(horizontal: 20);

  static const section = SizedBox(height: 15);

  static const small = SizedBox(height: 5);
}
